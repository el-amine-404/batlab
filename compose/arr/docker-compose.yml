name: potato-arr
services:

  bazarr:
    image: ${IMAGE_BAZARR}:${TAG_BAZARR}
    container_name: bazarr
    volumes:
      - ${VOLUMES_ROOT}/bazarr:/config
      - ${DATA_ROOT}:/data
      - /etc/localtime:/etc/localtime:ro
    environment:
      - TZ=${TZ}
      - PUID=${PUID}
      - PGID=${PGID}
    networks:
      - home_server
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
      - DAC_OVERRIDE
      - FOWNER
    healthcheck:
      test: [ "CMD", "curl", "-L", "-f", "http://localhost:6767" ]
      interval: 45s
      timeout: 10s
      retries: 3
      start_period: 60s

  cleanuparr:
    image: ${IMAGE_CLEANUPARR}:${TAG_CLEANUPARR}
    container_name: cleanuparr
    volumes:
      - ${VOLUMES_ROOT}/cleanuparr:/config
      - ${DATA_ROOT}/torrents:/data/torrents:ro
      - /etc/localtime:/etc/localtime:ro
    environment:
      - TZ=${TZ}
      - PUID=${PUID}
      - PGID=${PGID}
      - PORT=${CLEANUPARR_WEBUI_PORT}
    networks:
      - home_server
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
      - DAC_OVERRIDE
      - FOWNER
    healthcheck:
      test: [ "CMD", "curl", "-L", "-f", "http://localhost:${CLEANUPARR_WEBUI_PORT}" ]
      interval: 45s
      timeout: 10s
      retries: 3
      start_period: 60s
    cpus: ${CLEANUPARR_CPUS:?set in hosts/<profile>/compose.env}
    mem_limit: ${CLEANUPARR_MEM_LIMIT:?set in hosts/<profile>/compose.env}
    mem_reservation: ${CLEANUPARR_MEM_RESERVATION:?set in hosts/<profile>/compose.env}
    mem_swappiness: 0
    memswap_limit: ${CLEANUPARR_MEM_LIMIT:?set in hosts/<profile>/compose.env}

  gluetun:
    image: ${IMAGE_GLUETUN}:${TAG_GLUETUN}
    container_name: gluetun
    volumes:
      - ${VOLUMES_ROOT}/gluetun:/gluetun
    environment:
      - VPN_SERVICE_PROVIDER=protonvpn
      - VPN_TYPE=wireguard
      - WIREGUARD_PRIVATE_KEY=${WIREGUARD_PRIVATE_KEY}
      - SERVER_COUNTRIES=${GLUETUN_SERVER_COUNTRIES}
      - VPN_PORT_FORWARDING=on
      - TZ=${TZ}
      - BLOCK_MALICIOUS=on
      - BLOCK_ADS=on
      - BLOCK_SURVEILLANCE=on
      - FIREWALL_OUTBOUND_SUBNETS=172.19.0.0/16
      - UPDATER_PERIOD=24h
      - UPDATER_VPN_SERVICE_PROVIDERS=protonvpn
      - VPN_PORT_FORWARDING_UP_COMMAND=/bin/sh -c 'wget -qO- --retry-connrefused --post-data "json={\"listen_port\":{{PORT}},\"current_network_interface\":\"{{VPN_INTERFACE}}\",\"random_port\":false,\"upnp\":false}" http://127.0.0.1:8080/api/v2/app/setPreferences || echo "FAILED to set qbittorrent listen_port {{PORT}}"'
    networks:
      - home_server
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "/gluetun-entrypoint", "healthcheck" ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 60s
      start_interval: 60s
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    extra_hosts:
      - "lidarr:172.19.10.13"
      - "radarr:172.19.10.12"
      - "readarr:172.19.10.14"
      - "sonarr:172.19.10.11"

  lidarr:
    image: ${IMAGE_LIDARR}:${TAG_LIDARR}
    container_name: lidarr
    volumes:
      - ${VOLUMES_ROOT}/lidarr:/config
      - /etc/localtime:/etc/localtime:ro
    environment:
      - TZ=${TZ}
      - PUID=${PUID}
      - PGID=${PGID}
    networks:
      home_server:
        ipv4_address: 172.19.10.13
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
      - DAC_OVERRIDE
      - FOWNER
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8686/ping" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  prowlarr:
    image: ${IMAGE_PROWLARR}:${TAG_PROWLARR}
    container_name: prowlarr
    depends_on:
      gluetun:
        condition: service_healthy
        restart: true
    volumes:
      - ${VOLUMES_ROOT}/prowlarr:/config
      - /etc/localtime:/etc/localtime:ro
    environment:
      - TZ=${TZ}
      - PUID=${PUID}
      - PGID=${PGID}
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
      - DAC_OVERRIDE
      - FOWNER
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:9696/ping" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    network_mode: service:gluetun

  qbittorrent:
    image: ${IMAGE_QBITTORRENT}:${TAG_QBITTORRENT}
    container_name: qbittorrent
    depends_on:
      gluetun:
        condition: service_healthy
        restart: true
    volumes:
      - ${VOLUMES_ROOT}/qbittorrent:/config
      - ${DATA_ROOT}/torrents:/data/torrents
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
      - WEBUI_PORT=${QBITTORRENT_WEBUI_PORT}
    network_mode: service:gluetun
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
      - DAC_OVERRIDE
      - FOWNER
    healthcheck:
      test: ping -c 1 www.google.com || exit 1
      interval: 60s
      retries: 3
      start_period: 20s
      timeout: 10s
    ulimits:
      nofile:
        soft: 65536
        hard: 131072
      nproc: 8192

  radarr:
    image: ${IMAGE_RADARR}:${TAG_RADARR}
    container_name: radarr
    volumes:
      - ${VOLUMES_ROOT}/radarr:/config
      - ${DATA_ROOT}:/data
      - /etc/localtime:/etc/localtime:ro
    environment:
      - TZ=${TZ}
      - PUID=${PUID}
      - PGID=${PGID}
    networks:
      home_server:
        ipv4_address: 172.19.10.12
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
      - DAC_OVERRIDE
      - FOWNER
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:7878/ping" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s


  readarr:
    image: ${IMAGE_READARR}:${TAG_READARR}
    container_name: readarr
    volumes:
      - ${VOLUMES_ROOT}/readarr:/config
      - ${DATA_ROOT}:/data
      - /etc/localtime:/etc/localtime:ro
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    networks:
      home_server:
        ipv4_address: 172.19.10.14
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
      - DAC_OVERRIDE
      - FOWNER
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8787/ping" ]
      interval: 45s
      timeout: 10s
      retries: 3
      start_period: 60s

  recyclarr:
    image: ${IMAGE_RECYCLARR}:${TAG_RECYCLARR}
    container_name: recyclarr
    read_only: true
    tmpfs: /tmp
    security_opt:
      - no-new-privileges:true
    user: ${PUID}:${PGID}
    networks:
      - home_server
    volumes:
      - ${VOLUMES_ROOT}/recyclarr:/config
      - ${CONFIGS_ROOT}/arr/conf/recyclarr:/config/configs:ro
    environment:
      - TZ=${TZ}
      - CRON_SCHEDULE=@daily
      - SONARR_API_KEY=${SONARR_API_KEY}
      - RADARR_API_KEY=${RADARR_API_KEY}

  sonarr:
    image: ${IMAGE_SONARR}:${TAG_SONARR}
    container_name: sonarr
    volumes:
      - ${VOLUMES_ROOT}/sonarr:/config
      - ${DATA_ROOT}:/data
      - /etc/localtime:/etc/localtime:ro
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    networks:
      home_server:
        ipv4_address: 172.19.10.11
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
      - DAC_OVERRIDE
      - FOWNER
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8989/ping" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  flaresolverr:
    image: ${IMAGE_FLARESOLVERR}:${TAG_FLARESOLVERR}
    container_name: flaresolverr
    environment:
      - LOG_LEVEL=info
      - LOG_HTML=false
      - CAPTCHA_SOLVER=none
      - TZ=${TZ}
    restart: unless-stopped
    network_mode: service:gluetun
    depends_on:
      gluetun:
        condition: service_healthy
        restart: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    # read_only is not possible here: undetected-chromedriver patches its own
    # binary at runtime. cap_drop + no-new-privileges + pids_limit still apply.
    tmpfs:
      - /tmp:size=512m,mode=1777
    pids_limit: 512

networks:
  home_server:
    external: true
    name: home_server
